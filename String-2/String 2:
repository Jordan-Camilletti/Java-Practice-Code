public String repeatSeparator(String word, String sep, int count) {
  String wrd="";
  for(int x=0;x<(count*2)-1;x++){
    if(x%2==0){
      wrd+=word;
    }else{
      wrd+=sep;
    }
  }
  return wrd;
}

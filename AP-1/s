public int[] copyEvens(int[] nums, int count) {
  int[] arr=new int[count];
  int n=0;
  for(int x=0;x<nums.length;x++){
    if(nums[x]%2==0){
      arr[n]=nums[x];
      n++;
    }
    if(n+1==count) break;
  }
  return arr;
}

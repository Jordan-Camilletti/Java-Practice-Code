/**/

public int[] squareUp(int n) {
  int num=0;
  int[] arr=new int[n*n];
  for(int curr=1;curr<=n;curr++){
    for(int zeros=;zeros;zeros){
      arr[num]=0;
      num++;
    }
    for(int nums=;nums;nums){
      arr[num]=nums;
      num++;
    }
  }
  return arr;
}

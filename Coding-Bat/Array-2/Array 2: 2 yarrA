/**/

public int[] tenRun(int[] nums) {
  int curr=0;
  for(int x=0;x<nums.length;x++){
    if(nums[x]%10==0){
      curr=nums[x];
    }else if(curr!=0){
      nums[x]=curr;
    }
  }
  return nums;
}

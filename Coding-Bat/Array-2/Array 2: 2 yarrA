/**/

public int[] tenRun(int[] nums) {
  int curr=1;
  for(int x=0;x<nums.length;x++){
    if(nums[x]%10==0){
      curr=nums[x];
    }else if(curr!=1){
      nums[x]=curr;
    }
  }
  return nums;
}

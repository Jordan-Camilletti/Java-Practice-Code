/**/

public boolean meth(int nums, int g1, int g2, int c){
  if(c==nums.length) return g1==g2;
  return(meth(nums,g1+nums[c],g2,c++)||meth(nums,g1,g2+nums[c],c++));
}

public boolean splitArray(int[] nums) {
  
}

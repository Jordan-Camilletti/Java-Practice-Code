/**/

public boolean groupSum5(int start, int[] nums, int target) {
  if(start>=nums.length) return (target==0);
  if(nums[start]==5) return(groudSum5(start+1,nums,target-5));
  return(groupSum5()||groupSum5());
}

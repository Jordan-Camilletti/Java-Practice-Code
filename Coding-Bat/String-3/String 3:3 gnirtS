/**/

public String notReplace(String str) {
  String arr[]=str.split("");
  for(int x=0;x<arr.length-1;x++){
    if(arr[x].equals("i")&&arr[x+1].equals("t")&&!Character.isLetter(arr[x-1])&&!Character.isLetter(arr[x+2])){
      
    }
  }
}

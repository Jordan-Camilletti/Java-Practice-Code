/**/

public String sameEnds(String str){
  for(int x=0;x<=str.length()/2;x++){
    if((str.substring(0,x)).equals(str.substring(str.length()-x-1,str.length()-1))) return str.substring(0,x);
  }
  return "";
}

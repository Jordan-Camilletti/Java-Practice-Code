/**/

public String notReplace(String str) {
  String newStr="";
  if(str.length()<=2){
    if(str.equals("is")) return("is not");
    return(str);
  }
  if((str.substring(0,3)).equals("is ")){
    str=str.substring(0,3)+"not "+str.substring(3,str.length());
  }
  /*for(){
    if(){
      
    }
  }*/
  if((str.substring(str.length()-3,str.length())).equals(" is")){
    str=str+" not";
  }
  return(str);
}

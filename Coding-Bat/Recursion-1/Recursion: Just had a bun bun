/**/

public String parenBit(String str) {
  if(str.length()<=1 || str.substring(0,1).equals(")")){
    return(")");
  }
  if(str.substring(0,1).equals("(")){
    return(str.substring(0,2)+parenBit(str.substring(2,str.length())));
  }
  return(parenBit(str.substring(1,str.length())));
}
